eureka:
  client:
    registry-fetch-interval-seconds: 15
    service-url:
      defaultZone: ${DOORJE_EUREKA}/eureka/
      
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false

ribbon:
  eureka:
    enable: true
  SocketTimeout: 60000
  ReadTimeout: 60000
